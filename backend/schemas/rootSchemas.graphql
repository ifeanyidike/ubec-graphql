type Query{
    questions: [Question]
}

type Question{
    _id: ID!,
    subject: ID!,
    questions: [question]
}

type question{
    _id: ID!,
    question: String!,
    options: [String]!,
    answer: String!
}